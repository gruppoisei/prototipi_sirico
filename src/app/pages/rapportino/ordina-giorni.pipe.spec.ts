import { OrdinaGiorniPipe } from './ordina-giorni.pipe';

describe('OrdinaGiorniPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdinaGiorniPipe();
    expect(pipe).toBeTruthy();
  });
});
